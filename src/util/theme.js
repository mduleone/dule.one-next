import colors from './colors';

const theme = {
  colors,
  fonts: {
    screenFont: 'Calibri, Candara, Segoe, Segoe UI, Optima, Arial, sans-serif',
    printFont: 'Cambria',
  },
};

export default theme;
